defmodule PathOfTrade.Repo.Migrations.CreateStashes do
  use Ecto.Migration

  def change do

    #create table(:stash, primary_key: false) do
    #  add :id, :text, null: false
    #  add :lastCharacterName, :text, null: false, size: 23
    #  add :public, :boolean, null: false
    #  add :stash, :text, null: false
    #  add :stashType, :text, null: false, size: 15
    #end

    #create table(:account, primary_key: false do
    #  add :accountName, :test, null: false
    #end
    
  end
end
